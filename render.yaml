services:
  - type: web
    name: mega-kart-3d
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: DATA_DIR
        value: /var/data
    disk:
      name: mega-kart-data
      mountPath: /var/data
      sizeGB: 1
